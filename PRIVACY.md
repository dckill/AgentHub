# AgentHub 隐私说明

最后更新：2026 年 7 月 24 日。

本文描述当前仓库和默认托管服务的技术行为，不构成对所有自托管部署的统一承诺。自托管运营者需要自行说明其日志、备份、保留期限、第三方服务和适用法律。

## 当前数据处理边界

AgentHub 客户端在数据离开设备前对会话内容、消息、制品、托管凭据和用户 KV 等敏感负载进行端到端加密。服务端保存和同步密文，但仍需要处理协议运行所需的未加密元数据，例如账号公钥、记录 ID、时间戳、序号、设备/会话关联、在线状态、推送令牌和配额计数。

服务端管理员仍可看到网络连接信息、请求时间、响应状态和其基础设施生成的日志。端到端加密不等于隐藏所有元数据，也不能保护已经在终端、日志、剪贴板、通知或接收方设备上出现的明文。

## 分析与订阅

当前源码没有集成 PostHog、RevenueCat 或其他实际发送使用分析、购买或订阅数据的客户端 SDK。同步协议保留了部分历史兼容字段，但当前构建不会因此自动向这些服务发送数据。

如果未来加入分析、崩溃报告、支付或订阅服务，必须先更新实现、设置页和本文，并明确列出数据字段、处理方、用途、保留期限和退出方式。

## 推送通知

启用推送后，服务端会保存设备的推送令牌并使用配置的推送服务进行投递。通知内容应保持最小化；用户和自托管运营者应假设推送服务能够观察投递元数据。

## 本地存储

- CLI 默认把本机状态保存在 `~/.agenthub`。
- Native/Tauri 凭据使用平台安全存储；Web 认证根密钥和凭据仅驻留内存，刷新或关闭页面后需要重新登录。
- 用户主动复制、下载或导出的内容可能进入剪贴板、下载目录、终端历史、系统备份或其他应用，这些副本不受服务端删除控制。

## 保留与删除

当前产品支持删除部分业务记录和推送令牌，但没有完整的“删除账号及全部关联数据”或“导出全部账号数据”接口。因此本文不承诺固定的 30 天删除期限，也不宣称应用已经提供完整的数据主体自助请求流程。

默认托管服务和自托管运营者应在对外提供服务前确定备份、日志、数据库和对象存储的保留政策，并提供可验证的联系与删除流程。

## 安全报告与联系

漏洞请按 [SECURITY.md](SECURITY.md) 使用 GitHub Private vulnerability reporting 私下报告，不要在公开 Issue 中粘贴凭据或用户数据。公开仓库尚未启用该能力前，安全联系渠道仍未就绪。

历史构建中可能出现 `https://agenthub.yzsd.asia/support`；该地址当前不能作为已验证的隐私或安全联系渠道。

## 变更

数据处理方式发生实质变化时，应在发布前同步更新本文和相关 UI，并在发行说明中标注。
