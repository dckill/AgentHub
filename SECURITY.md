# 安全政策

## 支持范围

安全修复优先覆盖当前 `master` 分支以及 npm 上最新的 `@artsum/agenthub` 版本。旧版本可能需要先升级后才能获得修复。

## 报告漏洞

请不要在公开 Issue 中披露未修复漏洞、可用攻击步骤、密钥或用户数据。

公开仓库启用 GitHub Private vulnerability reporting 后，请通过仓库的 **Security → Report a vulnerability** 私下提交。报告应包含受影响版本、影响范围、复现条件和建议缓解方式；请使用脱敏样例，不要附带真实凭据或用户内容。

在公开仓库尚未启用私有漏洞报告前，项目不应宣称安全报告渠道已经就绪，也不应开始公开发布。

## 响应原则

- 收到报告后先确认影响范围和是否需要临时缓解。
- 修复发布前不公开可直接利用的细节。
- 发布修复后在 GitHub Security Advisory 中说明受影响版本、修复版本和升级方式。
- 若漏洞涉及默认托管服务，同时评估密钥轮换、会话撤销和用户通知。
